auto-install-peers=false
strict-peer-dependencies=false
# Prefer hoisted layout (helps Windows/exFAT hosts that cannot create symlinks)
node-linker=hoisted
package-import-method=copy
# Lexar/exFAT cannot symlink workspace packages — inject (copy) instead
inject-workspace-packages=true
